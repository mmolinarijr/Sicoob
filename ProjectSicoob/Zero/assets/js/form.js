let id = (id) => document.getElementById(id);
let classes = (classes) => document.getElementsByClassName(classes);

let name = id("name"),
    email = id("email"),
    form = id("form"),

    form.addEventListener("submit", (e) => {
        e.preventDefault();
      });

      let validation  = (id, class, message) => {}

        //remove espacos em branco
        if  (id.value.trim() === "") {

        } else {

        }

        